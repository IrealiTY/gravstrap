{% set attributes = parse_attributes(attributes) %}
<ul class="dropdown-menu {{ attributes['class'] }}"{% include 'partials/_attributes.html.twig' with {'ignore': ['class']} %} aria-labelledby="dropdownMenuButton">
  {% for item in items %}
    {% if item is iterable %}
    <li class="dropdown-item">{% include 'basic/link.html.twig' with {'menu': item.menu, 'url': item.url} %}</li>
    {% else %}
    <li class="dropdown-item">{{ item }}</li>
    {% endif %}
  {% endfor %}
</ul>
